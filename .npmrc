# npm 配置
# 注意：xlsx 包存在已知安全漏洞，但暂无修复版本
# 该包仅用于管理员后台的 Excel 导入功能，不处理用户输入
# 风险等级：中等（仅内部使用）
audit-level=moderate

# 解决 @auth/kysely-adapter 与 kysely 版本冲突
# @auth/kysely-adapter@1.11.1 需要 kysely@^0.27.5
# 项目使用 kysely@^0.28.8，通过 overrides 和 legacy-peer-deps 解决冲突
legacy-peer-deps=true
