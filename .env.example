# ============================================================
# ZALEM 后台管理系统 - 环境变量模板
# ============================================================
#
# ⚠️ 安全警告：
# - 请勿将真实的 .env 文件提交到版本控制系统
# - 确保生产环境的 .env 文件包含安全的密钥值
# - 定期轮换 ADMIN_TOKEN
#
# ============================================================

# 数据库连接串（PostgreSQL）
# 格式：postgresql://user:password@host:port/database
# 示例：postgresql://user:pass@localhost:5432/drivequiz
DATABASE_URL=postgresql://user:password@host:port/database

# Vercel/其他平台可能使用 POSTGRES_URL 作为别名
POSTGRES_URL=${DATABASE_URL}

# 管理员鉴权 Token
# - 用于所有 /api/admin/** 接口的 Bearer Token 认证
# - 建议使用强随机字符串（至少32字符）
# - 示例：openssl rand -hex 32
ADMIN_TOKEN=your-secure-admin-token-here

# 服务器时区（建议设置为 UTC）
# - 确保时间字段统一使用 UTC
# - 部署到 Vercel 时自动设置为 UTC
TZ=UTC
