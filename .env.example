# 环境变量配置模板
# 
# 使用说明：
# 1. 复制此文件为 .env.local（本地开发）或 .env（生产环境）
# 2. 填入实际的配置值
# 3. 不要将 .env 或 .env.local 文件提交到 Git

# ==========================================
# 数据库配置
# ==========================================

# Supabase 数据库连接字符串
# 格式: postgresql://postgres:PASSWORD@db.PROJECT_REF.supabase.co:5432/postgres?sslmode=require
# 获取方式：
# 1. 登录 Supabase Dashboard
# 2. 进入项目 → Settings → Database
# 3. 选择 Direct connection (URI 格式)
# 4. 复制连接字符串
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres?sslmode=require

# ==========================================
# 管理员配置
# ==========================================

# 管理员 Token（用于后台管理 API 鉴权）
# 可以设置任意字符串作为 Token，用于保护管理 API
ADMIN_TOKEN=your-admin-token-here

# ==========================================
# 系统配置
# ==========================================

# 服务器时区（建议使用 UTC）
TZ=UTC
