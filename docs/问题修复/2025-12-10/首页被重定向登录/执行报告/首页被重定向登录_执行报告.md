## 执行报告：首页被重定向登录

### 修改文件
- `src/config/authRoutes.ts`（新增：统一登录保护前缀与判断函数）
- `src/middleware.ts`（改用共用前缀判定，逻辑不变）
- `src/components/AuthGuard.tsx`（仅对需要登录的前缀做客户端重定向，其它路径放行）

### 核心变更说明
- 单一事实源：`AUTH_REQUIRED_PREFIXES` 与 `isAuthRequiredPath` 提取到 `src/config/authRoutes.ts`。
- 中间件继续按前缀保护学习/考试/AI/profile/admin，信息页放行。
- AuthGuard 只在命中登录保护前缀且未登录时跳转 `/login?callbackUrl=当前URL`，首页与信息页不再被客户端强制重定向。

### 自测结果
- 未登录：`/`、`/license`、`/services`、`/vehicles`、`/about`、`/terms`、`/privacy` 正常打开；`/study/learn`、`/license/study/xxx`、`/license/exam/yyy`、`/ai`、`/admin` 跳转登录并带回跳。
- 已登录：以上信息页正常；学习/考试正常进入；`/ai` 行为与既有激活流程一致。
- 构建：`npm run lint`、`npm run build` 通过（保留历史 warning，无新增 error）。

### 红线自检（节选）
- A1 路由无业务逻辑：已遵守。
- E4/E7 单一入口 & 最小变更：已收敛到共用判定函数。
- F1 AI 模块未改动。

### 后续风险
- 如新增需登录的前缀，需同步更新 `src/config/authRoutes.ts`，保证 AuthGuard 与 middleware 一致。
